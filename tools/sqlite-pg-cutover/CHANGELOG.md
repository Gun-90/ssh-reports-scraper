# Changelog

## 0.1.0

- Initial local package scaffold.
- Add `plan`, `ddl`, `copy`, `compare`, and `all` CLI commands.
- Add SQLite schema inspection and lowercase PostgreSQL DDL generation.
- Add batch copy and basic count/sample comparison.
